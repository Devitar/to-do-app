{
    "devDependencies": {
        "gh-pages": "^3.2.3"
    },
    "scripts": {
        "build": "elm-app build",
        "deploy": "gh-pages -d build",
        "watch-scss": "sass src/main.scss public/main.css -w"
    }
}